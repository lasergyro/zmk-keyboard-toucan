
- [x] trace the behavior of the touchpad state machine from the code and compare to the design: where can it fail? there is at least one confirmed instance of the PAD layer remaining active even seconds after finger liftoff.
  - **Root cause:** DRAG_WINDOW→TAP_PENDING transition ([input_pinnacle.c:485](external/cirque-input-module/drivers/input/input_pinnacle.c)) was missing `k_work_cancel_delayable(&data->pad_off_work)`. Orphaned pad_off timer fired after state changed to TAP_PENDING; guard check skipped BTN_TOUCH=0; PAD layer stuck on indefinitely. Fixed by adding the cancellation before the state assignment.
- [x] finish removing color values that can be derived from (possibly leader) layers from the css config.yaml section by the drawing generator.
  - Already complete: all layer/leader colors live in `toucan.annotation_layers` / `toucan.leader_namespaces` and are generated by generate-keymaps.rb. Static `svg_extra_style` block has only theme colors.
- [x] change the 'bt' label being still used in some places for the sys leader layer to 'sys'.
  - Renamed in draw/config.yaml (leader_namespaces key, raw_binding_map, zmk_combos), config/toucan.keymap (macro label), config/combos.dtsi (combo name), config/leader.dtsi (sequence names). Actual `&bt BT_SEL`/`&bt BT_CLR` bindings unchanged.
- [x] Redefine the leader sequences for OS modes as part of the SYS leader
      layer (was BT/USB layer). Use a gear icon ($$mdi:cog$$) for the V+B combo.
      &leader by itself does nothing — the Q+W combo that fired it directly is
      removed; leader is only triggered via namespace combo macros
      (greek_ns / german_ns / bt_ns). plan.md updated.
- [x] Remove ellipsis (...) from greek/german combo icons — now "α" and "ä".
- [x] Replace ⇧⌥Tab legend with a window icon ($$mdi:window-maximize$$).
- [x] Position the cut combo indicator further below copy/paste
      (combo_cut: { align: bottom, offset: 0.6 } in draw/config.yaml).
- [x] Backspace uses the macOS ⌫ symbol (BSPC in zmk_keycode_map).
- [x] Del uses a trash-can icon ($$mdi:trash-can$$).
- [x] Space uses ␣ (U+2423 OPEN BOX).
- [x] Prefer Apple-style symbols for keys: Esc ⎋, Enter ⏎, Tab ⇥.
- [x] Studio uses a paintbrush icon ($$mdi:brush$$); Reset uses $$mdi:restart$$;
      Bootloader uses $$mdi:flash$$.
- [x] Multi-glyph labels (MDI glyph + text suffix such as $$mdi:bluetooth$$1)
      now share the same colour. Added `use.left/right/tl/tr/bl/br` CSS rules
      that mirror the corresponding `text.*` colours — glyph `<use>` elements
      pick up the annotation colour just like their text siblings.
- [x] Home-row mod (HRM) labels are bigger and bolder
      (text.hold { font-size: 11px; font-weight: 900 }).
- [x] Corner labels given more breathing room
      (draw_config.small_pad: 5 → corner offset ±21 instead of ±24).
- [x] Layer activator keys (Fn, Nav, Num) rendered in the layer colour
      (keypos-36 → blue, keypos-37 → yellow, keypos-41 → orange).
- [x] Region-crop helper for automated visual debugging:
      `ruby draw/crop-keymap.rb <in.svg> <out.svg> "A"`  (or "D,F", "pos:36", …).
- [x] Leader-namespace combo indicators coloured like their annotation layer
      (greek/german combos → green #5e9262, sys combo → blue #458588). Done
      via zmk_combos type tags (greek-combo / german-combo / sys-combo) plus
      matching CSS rules in svg_extra_style.
- [x] OS mode keys in the SYS leader layer (E, R, T) now use MDI glyphs
      ($$mdi:linux$$, $$mdi:apple$$, $$mdi:apple-ios$$) via leader.dtsi
      comments.
- [x] Bluetooth + number labels render correctly. Patched
      `utils.py::_draw_legend` so the mixed "$$glyph$$suffix" pattern emits
      the suffix with `text-anchor:start` and a small negative gap
      (`-width * 0.15`) overlapping the empty MDI viewBox padding — glyph
      and number sit side-by-side on the same baseline, tight against the
      key edge.
- [x] Modifier scaling + Apple-style shift. HRM modifiers now use
      apple-keyboard-{command,option,control,shift} — the shift glyph is
      the wider/shorter Apple-keycap arrow. Annotation glyphs bumped via
      `draw_config.glyph_shifted_size: 12` so icons read as legibly as the
      adjacent 11px text.
- [x] Fn, Nav, and Num thumb-key labels now render in their layer colour
      in `keymap.svg` too (blue/yellow/orange). Widened the CSS selector
      to also match `text.tap` on keypos-36/37/41 — base.svg tags those
      keys with `layer-activator`, but keymap.svg only uses `key tap`.
- [x] create OS-aware clipboard behavior for copy/cut/paste combos — sends
      (ctrl|gui depending on selected OS) then the key (x/c/v) with proper
      press/release ordering. New `behavior_toucan_clipboard.c` behavior
      replaces the old `LC(INS)` / `LS(INS)` combos.
- [x] add .gitignore to the ./draw folder, to ignore generated files
      (*.svg, *.raw.yaml, base.yaml, keymap.yaml).
- [x] rework render config: generate-keymaps.rb now iterates over all layers
      dynamically (reads `toucan.annotation_layers` from config.yaml),
      auto-detects layer-activator keys via `{{class:NAME-activator}}` CSS
      tags (no more hardcoded keypos-36/37/41), and follows `#include`
      directives for leader sequence files (fixing missing Greek annotations).
- [x] move ./model and ./external/qmk_firmware (reference-only directories)
      to ./references/. Updated .gitignore accordingly.