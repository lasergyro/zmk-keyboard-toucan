# TODOs
- [x] Make file references in .md files clickable (use repo root relative paths wrapped in double square brackets).
- [x] Add config options to toggle each pad gesture; set to off by default. Adjust tests accordingly so they enable the right gesturest during testing.
- [x] Move Pan in the PAD layer to the rightmost thumb cluster key in the right half.
- [x] Produce a human_interface.md doc where you detail the constraints and goals of using the toucan keyboard, ie design guidelines for the keymap of each layer to be able to efficiently and reliably output the necessary key and mouse events, combos and sequences. Eg, which keys need to be transparent in the pad layer to be able to emit mouse clicks plus modifiers, where should click buttons be placed if the gestures are disable (due to being hard to trigger correctly, a possibility i am considering).
- [x] Split the `debug.sh` and `release.sh` scripts into separate build and upload commands.
- [x] Add mouse acceleration or whichever is the right equivalent for a touchpad, expose parameters through the live config mechanism; it should be possible to make the mouse cross the entire screen when moving the finger at human speed fast from one side to the other of the touchpad. Search online and ground your decisions by writting first a research doc named touchpad_acceleration.md.
- Currently pressing the + key on safari using the toucan pad (simple tap) results weirdly in safari interpreting as right click. Using the trackpad creates a tab when tapping.
- If I start a drag gesture on the pad, then hold shift, then hold PAN, the touchpad seems to lock into PAN mode and not exit.